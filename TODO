* MySpace: Get the user ID by parsing the Profile URL and remove :MySpaceName configuration option.
* Get all the orders that have a past timestamp at once, and merge them to call Locations just once per execution.
* Add GoogleBuzz statistics.